services:
  app:
    image: 
    container_name: yangcheng-tracehub
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    volumes:
      - ./data/prisma:/app/prisma
      - ./data/uploads:/app/public/uploads
